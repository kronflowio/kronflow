## Prerequisites
- JDK 21+
- Maven 3.9+

## Build
mvn clean verify

## Conventions
- Google Java Format enforced via Spotless
- All public APIs must have Javadoc
- Every feature branch must have unit tests

## PR Process
- Fork → branch → PR against main
- At least one passing CI run required
